# AGENTS.md

## Cursor Cloud specific instructions

This repository (`forexWebsiteSam`) is currently an empty scaffold — it contains only a `README.md`. There is no application code, no dependency manifest (`package.json`, `requirements.txt`, etc.), and no services to run.

### Current state
- **Language / framework**: Not yet chosen.
- **Services**: None.
- **Lint / test / build / run**: No commands available yet. Once code is added, update this section accordingly.

### Notes for future agents
- The update script (`echo "No dependencies to install"`) is a no-op placeholder. Replace it once a dependency manifest is added.
- When code is added, update this file with service descriptions, startup commands, lint/test/build instructions, and any non-obvious caveats discovered during setup.
